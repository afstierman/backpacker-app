type CityDestination {
  id: String!
  name: String!
  country: String!
  region: String!
  dailyCostUSD: Float!
  lat: Float!
  lng: Float!
  minZoom: Int!
}

type Query {
  cities: [CityDestination!]!
}
